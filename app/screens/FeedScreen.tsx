import { View } from "react-native";
import { Text } from "react-native-paper";

const FeedScreen = () => {
  return (
    <View>
      <Text>Feed</Text>
    </View>
  );
};

export default FeedScreen;
